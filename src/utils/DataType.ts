enum DataType {
  String = 'varchar',
  Integer = 'int',
  Decimal = 'decimal',
  Enum = 'enum',
  Boolean = 'boolean',
  Date = 'date',
  Time = 'time',
  DateTime = 'datetime',
  Timestamp = 'timestamp',
  Percentage = 'percentage',
}

export default DataType;
